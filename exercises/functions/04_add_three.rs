// 04_add_three.rs
// Write a function `add_three` that takes three `i32` arguments
// and returns their sum as the last expression (no `return` keyword).
// Display the result in `main()`.

fn add_three(a: i32, b: i32, c: i32) -> i32 {
    // TODO: return the sum of all three parameters
    0
}

fn main() {
    println!("{}", add_three(1, 2, 3));
}
