// 03_cube.rs
// Create a function called `cube` that returns `n * n * n` for an input `i32`.
// In `main()`, print the cubes of a few test values.

fn cube(n: i32) -> i32 {
    // TODO: return the cube of `n`
    n
}

fn main() {
    println!("{}", cube(2));
}
