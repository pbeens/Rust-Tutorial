// 02_print_sum.rs
// Write a function named `print_sum` that accepts two `i32` parameters
// and prints their sum using `println!`. Call it from `main()` with
// several different pairs of numbers.

fn print_sum(a: i32, b: i32) {
    // TODO: print the sum of `a` and `b`
    println!("{} {}", a, b);
}

fn main() {
    print_sum(1, 2);
}
